<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<meta name="description" content="SessionDB gives your team secure, AI-powered access to MySQL and Postgres—without sharing a single password." />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />
		<title>SessionDB — The Open Source Database Proxy for Modern Teams</title>
	</head>
	<body>
		<slot />
	</body>
</html>

<style is:global>
	*,
	*::before,
	*::after {
		box-sizing: border-box;
	}

	:root {
		--color-primary: #2563eb;
		--color-primary-hover: #1d4ed8;
		--color-text: #111827;
		--color-muted: #6b7280;
		--color-border: #e5e7eb;
		--font-sans: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
	}

	html,
	body {
		margin: 0;
		width: 100%;
		min-height: 100%;
		font-family: var(--font-sans);
		color: var(--color-text);
	}
</style>
