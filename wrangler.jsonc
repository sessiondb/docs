{
  // Static Astro site. Cloudflare Pages may run "wrangler deploy" — this makes it work.
  // For local/CI, prefer: npx wrangler pages deploy ./dist
  "name": "docs",
  "compatibility_date": "2025-03-10",
  "assets": {
    "directory": "./dist"
  }
}
